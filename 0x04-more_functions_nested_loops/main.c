#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int std1, std2, std3;

	std1 = addstudentscores(88, 89, 90);
	std2 = addstudentscores(88, 89, 90);
	std3 = addstudentscores(88, 89, 90);

	int addstudentscores(int x, int y, int z)
return (x + y + z);
}
